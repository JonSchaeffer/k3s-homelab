---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tracearr-data
  namespace: homelab
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
